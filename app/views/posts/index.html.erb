<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-2">
      <% if current_user %>
        <p><%= link_to 'Sign out', destroy_user_session_path %></p><br>
        <%= link_to 'New Post', new_post_path, class: "btn btn-primary" %>
      <% else %>
        <p><%= link_to 'Member Login', user_session_path %></p>
      <% end %>
    </div>
  </div>
  <br></br>

  <% @posts.each do |post| %>
    <div class="row">
      <div class="col-md-2">
        <%= image_tag post.image, class: "img-thumbnail" %>
      </div>
      <div class="col-md-10">
        <%= post.body.truncate(1000).html_safe %>
        <div class="text-muted">
          <em>Posted on <%= post.created_at.to_formatted_s(:long) %></em>
        </div>
        <% if current_user %>
          <p>
            <%= link_to 'Show', post %> |
            <%= link_to 'Edit', edit_post_path(post) %> |
            <%= link_to 'Destroy', post, method: :delete, data: {
            confirm: "Are you sure?" } %>
          </p>
        <% end %>
        <br><br>
        <br>
      </div>
    </div>
  <% end %>
</div>




